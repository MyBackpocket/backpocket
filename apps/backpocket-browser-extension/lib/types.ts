// Re-export types from shared package
// This ensures extension uses the same types as web and mobile
export type {
  APISave,
  CheckDuplicateResponse,
  Collection,
  CollectionVisibility,
  CreateSaveInput,
  DuplicateSaveInfo,
  Save,
  SaveVisibility,
  Space,
  Tag,
} from "@backpocket/types";
